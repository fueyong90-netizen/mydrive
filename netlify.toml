[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"  # Changé de "/opt/build/repo/frontend/frontend/dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
